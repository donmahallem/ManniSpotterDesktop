import { expect } from "chai";
import "mocha";
import { createApiNotFoundRequestHandler } from "./api-not-found-request-handler";

describe('api-server/api-not-found-request-handler', () => {
    describe('createApiNotFoundRequestHandler', () => {
        it('should test', () => {
            expect(true).to.equal(true);
        })
    })
});